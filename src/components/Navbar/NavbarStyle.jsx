import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#383850",
      light: "#ddaa27",
      dark: "#ac8a30",
    },
    secondary: {
      main: "#FFFFFF",
    },
  },
});

export default theme;
